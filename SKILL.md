---
name: write-outline
description: 글(에세이) 또는 강의·PPT '개요'를 집요한 인터뷰로 정련하는 오케스트레이터. 목적·의도·주제·의뢰서·자료(소재/레퍼런스)를 받아 '파악 보드'를 만들고, 빈칸만 집요하게 되물어(옵션우선) 핵심 메시지(강조점)부터 봉인한 뒤 칸별 신호등 게이트를 통과시켜 개요를 산출하고 분리된 검토자로 검수한다. "개요 짜줘/잡아줘", "이 주제로 글 쓰려는데 개요부터", "강의/PPT 개요 만들어줘", "의뢰서랑 자료 줄게 질문해줘", "deep-interview로는 부족해 더 집요하게 물어줘" 류 요청에 사용한다. 글 한 편 윤문은 humanize-korean, 지목 구절 수정은 passage-revision-loop, 과월호 레퍼런스 전면 정련은 seoul-edu-manuscript가 담당하며, 이 스킬은 '집필 전 개요 설계'에 특화된다.
---

# write-outline — 집필 전 개요 설계 인터뷰 하네스

글/강의 개요를 **집요한 인터뷰 → 파악 보드 → 핵심 메시지 관문 → 칸별 게이트 → 개요 → 검토**로 정련한다. `/deep-interview`가 "얕고 일찍 멈춘다"는 문제를 정조준해 고친 하네스다. 산출물은 **확정 개요**이며, 그 뒤(문장 윤문·구절 수정·슬라이드 제작)는 다운스트림 스킬로 인계한다.

> **자기완결형 글로벌 스킬.** 분리된 검토자는 `general-purpose` 에이전트에 인라인 프롬프트로 디스패치한다(프로젝트 로컬 에이전트에 의존하지 않는다). 프로파일별 상세(칸·루브릭·도전 탄알·검토 체크리스트)는 `references/`에 둔다.

## 0. 진입 라우터 — 첫 질문 (반드시 먼저)

> **"무엇을 쓰십니까?"** — ⓐ 일반 원고(에세이) / ⓑ 강의용 PPT 원고
> (자유 입력 허용. 사용자가 이미 밝혔으면 묻지 않고 진행.)

- **ⓐ → 프로파일 A:** `references/profile-a-원고.md` 로드.
- **ⓑ → 프로파일 B:** `references/profile-b-강의.md` 로드.
- 코어(6원칙·옵션우선·루브릭·신호등·관문·검토)는 공통. **칸 세트·게이트·도전 탄알만** 프로파일에서 갈아 끼운다.

## 1. 집요한-인터뷰 코어 6원칙 (매 라운드 강제)

1. **막연답 거부(ⓐ)** — 추상 답이면 점수 안 주고 좁히는 꼬리질문.
2. **3겹 파기(ⓑ)** — "왜 → 구체적으로(숫자·고유명사·장면) → 그래서 뭐가 달라지나" 최소 3단.
3. **게이트 전 산출 금지(ⓒ)** — 모든 하드 칸 🟢 전엔 개요 안 쓴다. ("딸깍" 생성 거부.)
4. **도전 모드 상시(ⓓ)** — 매 라운드 답의 약점·모순 1개를 *판단형 칸 한정*으로 친다. 탄알은 프로파일의 challenge bank에서.
5. **되짚기 봉인(ⓔ)** — 사용자 말을 *인용*해 "이렇게 이해했는데 맞습니까?"로 확정해야 그 칸 인정.
6. **해석안 제시 = 옵션우선(ⓕ)** — 백지 질문 금지. **모든 질문은 자료에서 뽑은 2~3개 후보를 먼저** 주고 고르거나 고치게 한다. 자유 입력 항상 열림. 후보는 들러리 아닌 진짜 다른 갈래.
   - **예외:** 핵심 메시지 칸은 옵션우선만으로 닫지 않는다(§3 관문 ⓒ 백지 예외).

**한 라운드 = 한 질문.** 가장 약한(🔴/🟡) 하드 칸을 골라 한 번에 하나만 묻는다.

### 라운드 운용 — 한 질문 원칙과 관문의 관계 (테스트 처방)
- **파악 보드 되짚기는 별도 라운드가 아니다.** 보드를 보여주며 "틀린 항목 있으면 먼저 지적해 주세요"라고 한 *같은 턴에* 곧장 핵심 메시지 관문으로 들어간다. 단 사용자가 ✅를 정정하면 반영 후 진행.
- **관문의 〈위계·역의심·백지〉는 한 턴에 묶는다**(위계 명시 + 후보 2~3개 + 백지 질문 = 한 질문). 세 라운드로 쪼개지 않는다.
- **관문 〈역기둥〉은 핵심 메시지 봉인 *직후* 한 턴.** 즉 핵심 메시지 칸은 보통 **2턴**(위계·역의심·백지 → 봉인+역기둥)에 닫힌다.
- **관문의 〈역의심〉이 곧 핵심 메시지 칸의 도전이다**(코어 원칙 4 도전과 같은 일). 핵심 메시지 칸에 원칙 4 도전을 *중복* 적용하지 않는다. 원칙 4 도전은 *핵심 메시지 외* 판단형 칸에서 매 라운드 적용.

> ⚠️ 라벨 주의: **코어 6원칙은 ⓐ~ⓕ, 관문 4단계는 이름(위계·역의심·백지·역기둥)으로 구분**한다. 같은 동그라미 글자를 두 곳에 쓰지 않는다.

### 압박 대응 — "빨리/그냥 개요만" 요청 (★ 테스트 처방)
시간 압박은 **압축**으로 받지 **생략**으로 받지 않는다.

**압축 경로 = 정확히 3턴. 첫 응답에서 이 3턴을 사용자에게 예고한다:**
1. **핵심 메시지 관문**(§3: 위계·역의심·백지·역기둥)
2. **사례·근거 쌍 한 묶음 확인** — 핵심 메시지 봉인 *직후*, 어느 사례에 어느 근거를 짝지을지 한 화면으로 확인(구조 골격도 여기서 후보 제시).
3. **개요 산출.**

- **절대 안 건너뛰는 2개 = 1턴과 2턴.** 둘 다 통과 전엔 개요(3턴) 없음.
- **"핵심 메시지만 정하면 바로 개요"는 금지** — 2턴(사례·근거 쌍)을 빠뜨린 *같은 구멍의 약한 변종*이다. 핵심 메시지 봉인 직후 "이제 개요"가 아니라 **"이제 사례·근거를 한 번에 맞추고 → 개요"**라고 말한다.
- **압축해도 되는 것:** 칸별 *다회* grill → *후보 묶음 1회 확인*(여러 칸 후보를 한 화면에 모아 한 번에 고르게). 라운드 수는 줄이되 게이트는 유지.
- **딸깍 금지:** "나머지는 추정해서 바로 개요" = 생성 금지. 사용자가 명시 승인해도 봉인된 척추·사례근거 없이는 산출하지 않고, 추정으로 채운 칸은 🟡·개요에 `[추정]` 표시.

| 압박 핑계 | 실제 |
|----------|------|
| "마감이라 질문은 사치" | 강조점 없는 6쪽이 더 큰 낭비. 압축 경로는 3턴이면 닫힌다. |
| "핵심만 정하면 바로 개요 줘" | 사례·근거 빠진 개요는 검토(§7)에서 터진다. 2턴(사례·근거)을 건너뛰지 않는다. |
| "나머지는 알아서 추정해서 개요로" | 추정 직행 = 딸깍 생성. |

## 2. 루프 (7단계)

```
[1 덤프]   목적·의도·주제 + 의뢰서 + 자료. 자료는 두 종류로 가른다:
           · 소재 자료(보고서·데이터) = 내용·사실·근거의 원천
           · 레퍼런스(본받을 기사·잘 된 PPT) = 형식·문체·구성의 본보기 (선택)
[2 파악]   자료 정독 → 파악 보드(✅확정/🟡추정/⬜빈칸 + 출처 라벨·신뢰 등급) + (있으면) 레퍼런스 분석 보드 → 사용자에게 보여주고 되짚기
[3 관문]   핵심 메시지(강조점) = 선결 관문. §3 하위 프로토콜로 먼저 봉인 (다른 칸 grill 전)
[4 grill]  나머지 하드 칸을 빈칸·약한 순으로 집요하게 되물음(옵션우선·도전·봉인). 자료에 이미 있는 건 안 묻는다
[5 게이트] 모든 하드 칸 🟢이면 통과 (칸별 신호등; 단일 임계 가중합 아님)
[6 개요]   섹션별 개요 산출 — 각 섹션: 무슨 주장 + 어떤 사례 + 어떤 근거
[7 검토]   인터뷰어와 분리된 검토자(general-purpose) 디스패치 → 체크리스트 대조 → severity 라우팅
```

### [2] 파악 보드 + provenance
- 두 자료를 읽어 알려진 사실을 ✅/🟡/⬜로 분류. **각 ✅·🟡에 출처 라벨**(`[의뢰서]`/`[소재자료]`/`[레퍼런스]`/`[사용자발화]`/`[AI추정]`)**과 신뢰 등급**(`:확정`/`:웹`/`:추정`)을 단다.
- **`:추정`은 반드시 "제 추정입니다 — 맞습니까?" 확인을 띄운다**(되짚기 전 ✅ 승격 금지). ✅는 출처 행을 한 줄 인용. → 보드가 자료를 *오독해 확정으로 굳히는* 사고를 막는다.
- 레퍼런스가 있으면 **구성·형식·문체** 3축으로 분석(레퍼런스 프로파일). 활용은 **형태 칸(구조·형식·톤)만** 먹이고 **강조점엔 절대 안 닿는다** — 그대로 베끼면 남의 글의 복제.

### [3] 핵심 메시지 관문 — 선결 (★ 가장 중요)
강조점은 척추다. 자료의 *자기 표제*에 끌려가면 사용자가 이미 버린 프레임에 봉인된다. 닫기 전 아래 네 단계를 *순서대로*(라벨은 이름으로, ⓐ~ⓕ는 코어 원칙 전용):
- **〈위계〉 문서 위계** — 후보 랭킹은 **바인딩 문서(의뢰서·기획 의도) > 원천 자료(보고서·데이터)**. 원천 보고서의 자기 제목·주제어를 1순위로 자동 배정하지 않는다. 의뢰서 방향 ≠ 보고서 방향이면 충돌을 명시("의뢰서는 A, 보고서는 B를 가리킵니다 — 무엇이 척추입니까?").
- **〈역의심〉 프레임 역의심** — "보고서 표제 = 강조점" 가정을 일부러 1회 친다: "이 강조점은 자료가 이미 단 라벨일 뿐, 진짜 하려는 말이 아닐 수 있습니다. 표제를 그대로 쓰면 안 되는 이유는?"
- **〈백지〉 백지 예외** — 자료 후보 2~3개 제시 *직후* 옵션우선을 깨고 백지 질문 1회: **"이 셋 중 어느 것도 아닌, 끝내 남기고 싶은 한 문장은?"** 자료 *너머*를 묻는 유일한 통로.
- **〈역기둥〉 역기둥 탐지** — 주 강조점이 🟢 후보가 되면 한 번 더: **"이 주 논지를 떠받치거나 반박하는 둘째 기둥이 있습니까?"** 이대형 글은 흔히 *주 논지 + 역기둥*의 이중 구조. 있으면 별도로 박아 구조에 자리를 준다. **역기둥은 1개로 수렴**한다 — 사용자가 둘 이상을 지목하면 한 문장으로 통합하거나 "주 역기둥 1 + 보조 1"로 정리해 *되짚기로 봉인*(통합본을 인용해 확인). 미봉인 복수 역기둥을 들고 다음으로 넘어가지 않는다.
→ 위계·역의심·백지·역기둥 + 되짚기 봉인(코어 원칙 5) 끝나야 핵심 메시지 🟢. 그 전엔 다른 칸 grill 시작 안 함.

### [4~5] 칸 grill + 채점(신호등)
- 칸 세트·성격(판단형/사실형)·도전 탄알은 **프로파일 파일** 참조.
- **루브릭 사다리:** L0(빈칸)🔴 / L1(막연·천장)🔴 / L2(구체나 미봉인)🟡 / L3(구체+봉인+(해당 시)사례·근거+(판단형)도전통과)🟢.
- **판단형** 칸은 도전, **사실형** 칸은 자료서 확정되면 바로 L3(도전 면제).
- **칸 처리 순서(테스트 처방):** 핵심 메시지·역기둥·비목표·독자 봉인 → **구조(⑤) 골격 먼저** → 그 골격의 섹션마다 사례(③)·근거(④)를 *쌍으로* 채운다. **사례·근거 쌍은 구조가 정해진 뒤라야 검증된다** — ③·④를 구조 전에 독립 🟢로 올리지 않는다.
  - **레퍼런스가 없으면 구조(⑤)·톤은 빈칸이 아니라 인터뷰어가 후보 제시**: 구조 후보는 *사례의 시간·인과 사슬*에서 뽑고(레퍼런스 대신 사례 내재 순서가 근거), 톤은 의뢰서·매체 규범에서 뽑아 옵션우선으로 묻는다.
- **종료 게이트:** 핵심 메시지 🟢(선결) → 나머지 모든 하드 칸 🟢 → 통과.

### [6] 개요
- 모든 하드 칸 🟢일 때만 산출. 섹션별 {주장·사례·근거}.
- **근거 유형:** 개요 전에 "수치형 vs 서사형" 판정. 근거가 약하면 "채워라"만이 아니라 "(수치)프레임을 버리고 장면·인용으로 가라"도 동등 후보.
- **섹션 순서:** 강조점 랭킹이 아니라 **사례 자체의 시간·인과 사슬**을 따른다.

### [7] 검토 — 분리된 검토자
`general-purpose` 에이전트를 디스패치하되 **인터뷰 산출 개요 + 프로파일 체크리스트**를 주고 검수시킨다(인터뷰어가 자기 산출을 자가채점하지 않게). 검토자 프롬프트에 프로파일 파일의 "§검토 체크리스트"를 그대로 넣는다.
- **severity 라우팅:** 🔴 구조결함(강조점 라벨베낌·요소 누락·사례-근거 깨짐·역기둥 누락) → **관문/칸 재오픈**. 🟡 개요결함(순서·비중·중복) → **국소수정** 후 재검토. 🟢 문장씨앗(표현 차원) → **다운스트림 인계**.
- 🔴·🟡 0건이면 개요 확정.

## 3. 인계 (개요 확정 후)
- **글(A):** 문장 윤문 → `humanize-korean` / 지목 구절 정밀수정 → `passage-revision-loop`(있으면) / 과월호 레퍼런스 전면 정련 → `seoul-edu-manuscript`(서울교육 한정).
- **강의(B):** 슬라이드 제작·문구 → `leedaehyung-pptx`·`lecture-redesign-harness`·`lecture-deck-builder`.
- 인계는 **소프트**: "개요 확정됐습니다. 다음으로 문장 윤문(humanize-korean)으로 넘길까요?"처럼 제안하되 강제하지 않는다.

## 4. 산출물 & 수정 패턴
- 확정 개요(섹션별 주장·사례·근거 + 근거 유형 + 강조점·역기둥) + 인터뷰 기록을 제시.
- 작업 폴더에 `_workspace/`가 있으면 개요를 저장. `revision_pattern.md`가 있으면 인터뷰에서 드러난 사용자 선호를 누적.

## 5. 사용자 확인 지점
- 파악 보드 되짚기(되돌리기 쉬움) → 가볍게.
- 핵심 메시지 봉인·개요 확정(되돌리기 비쌈) → 반드시 사용자 확인.
- 무한 루프 방지: 같은 칸이 3라운드 넘게 안 닫히면 남은 쟁점을 보고하고 사용자에게 수동 봉인 여부를 묻는다(임의 종결 금지).

## 6. 설계 출처
이 스킬은 `이대형 글쓰기 하네스 설계 v1.5`의 구현이다. 백테스트·엔드투엔드 테스트로 검증됨(가드레일=신뢰, 긍정적 착지=사람 편집 전제). 차용: `deep-interview`(모호도 수학·1질문/라운드·challenge), `k-teacher-skills`(provenance·non-goals·옵션우선·assessment-first).
